import { MethodologyPlaceholder } from "@/components/MethodologyPlaceholder";

export default function MetodologiaPage() {
  return (
    <main className="prevent-shell methodology-shell">
      <MethodologyPlaceholder />
    </main>
  );
}
